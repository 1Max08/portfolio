<?php
if (!isset($_SESSION)) session_start();
if (!isset($_SESSION['user'])) {
    header("Location: index.php?page=login");
    exit;
}
?>

<div class="change">
    <h2>Modifier le projet</h2>

    <form method="POST">
        <p>Titre:</p>
        <input type="text" name="titre" value="<?= htmlspecialchars($project['titre'] ?? '') ?>" required/>

        <p>Description:</p>
        <textarea name="description" required><?= htmlspecialchars($project['description'] ?? '') ?></textarea>

        <p>Image (URL):</p>
        <input type="text" name="image" value="<?= htmlspecialchars($project['image'] ?? '') ?>" required/>

        <p>Description courte:</p>
        <textarea name="shortDescription" required><?= htmlspecialchars($project['shortDescription'] ?? '') ?></textarea>

        <button type="submit">Sauvegarder les modifications</button>
        <a href="index.php?page=board">
            <button type="button">Retourner</button>
        </a>
    </form>
</div>