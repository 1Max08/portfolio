<?php
if (!isset($_SESSION)) session_start();
if (!isset($_SESSION['user'])) {
    header("Location: index.php?page=about");
    exit;
}
?>

<div class="create">
    <h2>Créer un projet</h2>

    <form method="POST">
        <p>Titre:</p>
        <input type="text" name="titre" value="<?= htmlspecialchars($_POST['titre'] ?? '') ?>" required/>

        <p>Description:</p>
        <textarea name="description" required><?= htmlspecialchars($_POST['description'] ?? '') ?></textarea>

        <p>Image (URL):</p>
        <input type="text" name="image" value="<?= htmlspecialchars($_POST['image'] ?? '') ?>" required/>

        <p>Description courte:</p>
        <textarea name="short_description" required><?= htmlspecialchars($_POST['short_description'] ?? '') ?></textarea>

        <button type="submit">Créer</button>
    </form>

    <a href="index.php?page=board">
        <button type="button">Retourner</button>
    </a>
</div>